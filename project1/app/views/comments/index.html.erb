<!DOCTYPE html>
<html>
<head>
<title>Comments</title>
<style>
  body { background-color: #1F3D4C;
      background-image: url("http://upload.wikimedia.org/wikipedia/commons/thumb/6/64/MBTA.svg/500px-MBTA.svg.png");
      background-repeat: no-repeat;
      background-position: right top}

  h1   {color:black}
  p    {color:black}

    a:link {
    color:#000000;
    background-color:transparent;
    text-decoration:underline;
}
a:visited {
    color:#000000;
    background-color:transparent;
    text-decoration:underline;
}
a:hover {
    color:#ff0000;
    background-color:transparent;
    text-decoration:underline;
}
a:active {
    color:#ff0000;
    background-color:transparent;
    text-decoration:underline;
}

  </style>
</head>

<body>

<h1>All Comments</h1>
<p>Make sure to include the line color in your title!</p>

<%= form_for :comment,  url: comments_path do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit %>
  </p>
  <% end %>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>

  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.title %></td>
      <td><%= comment.body %></td>
    </tr>


  <% end %>
</table>

<br>

<%= link_to 'Home', controller: 'welcome' %>

</body>

</html>
